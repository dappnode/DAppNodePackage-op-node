version: "3.5"
services:
  op-node:
    build:
      context: op-node
      args:
        UPSTREAM_VERSION: op-node/v1.5.1-rc.1
    environment:
      - CUSTOM_L1_RPC
    restart: unless-stopped
    image: "op-node.op-node.dnp.dappnode.eth:0.1.1"
